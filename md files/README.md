# Система мониторинга нарушений

Система автоматического мониторинга нарушений с использованием компьютерного зрения, онтологии и Telegram бота.

## Возможности

- 📹 Мониторинг камеры в реальном времени
- 🔍 Детекция нарушений:
  - Курение в запрещенных местах
  - Выброс мусора
  - Рисование граффити
- 🧠 Классификация через онтологию
- 📱 Автоматическая отправка уведомлений в Telegram с информацией о нарушении, статье и размере штрафа

## Установка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Узнайте ваш Chat ID (можно использовать [@userinfobot](https://t.me/userinfobot))

### 3. Конфигурация

Скопируйте файл `.env.example` в `.env` и заполните необходимые параметры:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
CAMERA_SOURCE=0  # 0 для веб-камеры, или путь к видео файлу
```

## Использование

### Запуск системы

```bash
python main.py
```

### Обучение вашей модели (smoking / littering / graffiti)

1) Убедитесь, что датасет разложен так:

- `dataset/images/train`, `dataset/images/val`
- `dataset/labels/train`, `dataset/labels/val`

2) Проверьте `data.yaml` (у вас уже есть) и запустите обучение:

```bash
python train_violation_model.py --data data.yaml --model yolov8n.pt --epochs 80 --device cpu
```

После обучения появится файл:
- `violation_detection/train/weights/best.pt`

### Как заставить Telegram-бота и камеру использовать вашу модель

Добавьте в `.env`:

```env
YOLO_MODEL_PATH=violation_detection/train/weights/best.pt
# Если в обученной модели перепутаны названия классов, можно переопределить их тут:
# YOLO_CLASS_NAMES=smoking,littering,graffiti
# Пример если модель показывает smoking как graffiti (то есть 0=graffiti,2=smoking):
# YOLO_CLASS_NAMES=graffiti,littering,smoking
```

И запустите:

- Бот для анализа фото в чате:

```bash
python bot_handler.py
```

- Мониторинг камеры:

```bash
python main.py
```

### Остановка

Нажмите `q` в окне с видео для остановки мониторинга.

## Структура проекта

```
violation_monitoring_system/
├── main.py                 # Главный модуль
├── camera_monitor.py       # Мониторинг камеры
├── detector.py             # Детекция нарушений (CV)
├── ontology.py             # Онтология нарушений
├── telegram_bot.py          # Telegram бот
├── config.py               # Конфигурация
├── requirements.txt         # Зависимости
├── .env.example            # Пример конфигурации
└── README.md               # Документация
```

## Важные замечания

### Обучение модели детекции

Текущая версия использует стандартную YOLO модель, которая **не детектирует специфические нарушения** (курение, выброс мусора, граффити). 

Для работы в продакшене необходимо:

1. **Собрать датасет** с изображениями нарушений
2. **Разметить данные** (аннотировать bounding boxes)
3. **Обучить YOLO модель** на ваших данных
4. **Обновить `detector.py`** для использования обученной модели

### Онтология

Онтология содержит информацию о нарушениях согласно КоАП РФ:
- **Курение**: Статья 6.24, штраф 500 руб.
- **Выброс мусора**: Статья 8.1, штраф 1000 руб.
- **Граффити**: Статья 7.17, штраф 5000 руб.

Размер штрафа может корректироваться на основе контекста (повторное нарушение, общественное место и т.д.)

## Разработка

### Добавление новых типов нарушений

1. Добавьте класс в `config.py` → `VIOLATION_CLASSES`
2. Добавьте информацию в `ontology.py` → `_initialize_violations_db()`
3. Обучите модель для детекции нового типа нарушения
4. Обновите `detector.py` для обработки нового класса

### Расширение онтологии

Онтология может быть расширена для:
- Учета местоположения
- Учета времени суток
- Учета повторных нарушений
- Интеграции с внешними базами данных

## Лицензия

MIT License

## Автор

Система разработана для автоматического мониторинга нарушений.
