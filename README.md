# Система мониторинга нарушений

Система автоматического мониторинга нарушений с использованием компьютерного зрения, онтологии и Telegram бота.

## Возможности

- 📹 Мониторинг камеры в реальном времени
- 🔍 Детекция нарушений:
  - Курение в запрещенных местах
  - Выброс мусора
  - Рисование граффити
- 🧠 Классификация через RDF/TTL онтологию (`violations_ontology.ttl`)
- 📱 Автоматическая отправка уведомлений в Telegram с информацией о нарушении, статье и размере штрафа

## Установка

### 1. Установка зависимостей

```bash
python -m pip install -r requirements.txt
```

### 2. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Узнайте ваш Chat ID (можно использовать [@userinfobot](https://t.me/userinfobot))

### 3. Конфигурация

Создайте файл `.env` в корне проекта и заполните параметры:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
CAMERA_SOURCE=0  # 0 для веб-камеры, или путь к видео файлу

# RDF/TTL онтология (обязательно): статьи/штрафы берутся только из RDF
ONTOLOGY_TTL_PATH=violations_ontology.ttl

# Путь к вашей дообученной YOLO модели (best.pt)
# Пример: YOLO_MODEL_PATH=violation_detection/train8/weights/best.pt
YOLO_MODEL_PATH=

# Если в весах перепутаны имена классов, можно переопределить соответствие id->name:
# YOLO_CLASS_NAMES=graffiti,littering,smoking
```

## Использование

### Telegram-бот (вы отправляете фото в чат и получаете результат)

```bash
python bot_handler.py
```

### Обучение вашей модели (smoking / littering / graffiti)

#### 1) Подготовка датасета (train/val)

Если у вас исходные файлы лежат в `dataset/images` и `dataset/labels` (в корне папок), выполните:

```bash
python prepare_dataset.py --train-ratio 0.8 --seed 42 --clean
```

Ожидаемая структура:

- `dataset/images/train`, `dataset/images/val`
- `dataset/labels/train`, `dataset/labels/val`

#### 2) Проверьте `data.yaml`

Важно: порядок `names:` должен соответствовать **числам классов** в ваших `.txt` разметках.

#### 3) Запуск обучения

```bash
python train_violation_model.py --data data.yaml --model yolov8n.pt --epochs 80 --device cpu --batch 8 --workers 0
```

После обучения появится:
- `violation_detection/train/weights/best.pt` (или `train2`, `train3` и т.д., если папка уже существовала)

### Как заставить Telegram-бота и камеру использовать вашу модель

Добавьте в `.env`:

```env
YOLO_MODEL_PATH=violation_detection/train/weights/best.pt
# Если в обученной модели перепутаны названия классов, можно переопределить их тут:
# YOLO_CLASS_NAMES=smoking,littering,graffiti
# Пример если модель показывает smoking как graffiti (то есть 0=graffiti,2=smoking):
# YOLO_CLASS_NAMES=graffiti,littering,smoking

# RDF/TTL онтология (обязательно)
ONTOLOGY_TTL_PATH=violations_ontology.ttl
```

И запустите:

### Мониторинг камеры (и отправка уведомлений в Telegram)

```bash
python main.py
```

### Продолжить обучение (дообучение / resume)

Если вы добавили новые данные и хотите продолжить обучение с чекпойнта:

```bash
python train_violation_model.py --data data.yaml --model violation_detection/train/weights/last.pt --resume --epochs 120 --device cpu --batch 8 --workers 0
```

### Остановка

Нажмите `q` в окне с видео для остановки мониторинга.

## Структура проекта

```
violation_monitoring_system/
├── main.py                 # Главный модуль
├── camera_monitor.py       # Мониторинг камеры
├── detector.py             # Детекция нарушений (CV)
├── ontology.py             # Онтология (загрузка RDF/TTL)
├── violations_ontology.ttl  # RDF/TTL онтология (статьи/штрафы/описания)
├── bot_handler.py          # Telegram-бот: обработка фото из чата
├── telegram_bot.py         # Telegram notifier для мониторинга камеры
├── config.py               # Конфигурация
├── requirements.txt         # Зависимости
├── prepare_dataset.py      # Сплит датасета на train/val
├── train_violation_model.py # Обучение/дообучение YOLO модели
└── README.md               # Документация
```

## Важные замечания

### Обучение модели детекции

Система **поддерживает дообученную YOLO модель**. Чтобы реально распознавать ваши классы, укажите путь к весам:

- `YOLO_MODEL_PATH=.../best.pt`

### Онтология (RDF)

Справочная информация (статья/штраф/описание) хранится в `violations_ontology.ttl` и загружается через `rdflib`.

## Разработка

### Добавление новых типов нарушений

1. Добавьте класс в `config.py` → `VIOLATION_CLASSES`
2. Добавьте новую сущность в `violations_ontology.ttl` (ID должен совпадать с `type`, который выдаёт модель)
3. Дообучите модель для детекции нового типа нарушения

### Расширение онтологии

Онтология может быть расширена для:
- Учета местоположения
- Учета времени суток
- Учета повторных нарушений
- Интеграции с внешними базами данных

## Лицензия

MIT License

## Автор

Система разработана для автоматического мониторинга нарушений.
